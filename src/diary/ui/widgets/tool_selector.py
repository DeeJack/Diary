"""Enum with the tools available in the application"""

from enum import Enum


class Tool(Enum):
    """Enum with the tools available in the application"""

    PEN = "PEN"
    ERASER = "ERASER"
    TEXT = "TEXT"
    DRAG = "DRAG"
    IMAGE = "IMAGE"
    AUDIO = "AUDIO"
    SELECTION = "SELECTION"
